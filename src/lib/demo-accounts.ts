// The shared password every seeded demo persona uses. Intentionally
// public — it's documented in the README and printed on the login page
// itself, the same way any public demo/sandbox environment publishes its
// test credentials. Never used for a self-registered account; see
// `isDemoAccount` on the User model.
export const DEMO_PASSWORD = "password123";
